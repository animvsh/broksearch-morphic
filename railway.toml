[build]
builder = "nixpacks"
buildCommand = "bun run build"

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[ deployments ]
、健康檢查 = "/"

[ env ]
PORT = "8080"
